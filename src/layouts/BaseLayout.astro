---
import Analytics from "@vercel/analytics/astro"

interface Props {
  title: string
  description?: string
}

const {
  title,
  description = "Luxury Interior Design Services for Modern Homes",
} = Astro.props
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title} | Luxury Interior Design</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Poppins:wght@300;400;500;600&display=swap"
      rel="preload"
      as="style"
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Poppins:wght@300;400;500;600&display=swap"
        rel="stylesheet"
      />
    </noscript>
    <!-- LCP image optimization: use <img> with priority loading -->
  </head>
  <body>
    <header class="header" role="banner" aria-label="Main site header">
      <div class="container">
        <div class="logo">
          <a href="/">LUXE INTERIORS</a>
        </div>
        <nav>
          <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/services">Services</a></li>
            <li><a href="/portfolio">Portfolio</a></li>
            <li><a href="/contact">Contact</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <main>
      <slot />
      <Analytics />
    </main>

    <footer>
      <div class="container">
        <div class="footer-content">
          <div class="footer-logo">
            <h3>LUXE INTERIORS</h3>
            <p>Creating beautiful spaces that inspire</p>
          </div>
          <div class="footer-links">
            <h4>Quick Links</h4>
            <ul>
              <li><a href="/">Home</a></li>
              <li><a href="/about">About</a></li>
              <li><a href="/services">Services</a></li>
              <li><a href="/portfolio">Portfolio</a></li>
              <li><a href="/contact">Contact</a></li>
            </ul>
          </div>
          <div class="footer-contact">
            <h4>Contact Us</h4>
            <p>123 Design Street, Bangalore, India</p>
            <p>Email: info@luxeinteriors.com</p>
            <p>Phone: +91 98765 43210</p>
          </div>
        </div>
        <div class="footer-bottom">
          <p>
            &copy; {new Date().getFullYear()} Luxe Interiors. All rights reserved.
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  /* Global Styles */
  :root {
    --primary-color: #7d6e7a;
    --secondary-color: #3a3e59;
    --accent-color: #a67c73;
    --text-color: #22223b;
    --light-color: #f2e9e4;
    --dark-color: #22223b;
    --white-color: #ffffff;
    --transition: all 0.3s ease;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: "Poppins", sans-serif;
    color: var(--text-color);
    line-height: 1.6;
    background-color: var(--light-color);
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: "Playfair Display", serif;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  a {
    text-decoration: none;
    color: inherit;
    transition: var(--transition);
  }

  img {
    max-width: 100%;
    height: auto;
  }

  .container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Header Styles */
  .header {
    padding: 1.5rem 0;
    background-color: transparent;
    position: absolute;
    width: 100%;
    z-index: 10;
  }

  .header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo a {
    font-family: "Playfair Display", serif;
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--light-color);
    letter-spacing: 1px;
  }

  nav ul {
    display: flex;
    list-style: none;
    gap: 2rem;
  }

  nav a {
    color: var(--light-color);
    font-weight: 500;
    font-size: 1rem;
    padding: 0.5rem 0;
    position: relative;
  }

  nav a:after {
    content: "";
    position: absolute;
    width: 0;
    height: 2px;
    bottom: 0;
    left: 0;
    background-color: var(--accent-color);
    transition: var(--transition);
  }

  nav a:hover:after {
    width: 100%;
  }

  /* Footer Styles */
  footer {
    background-color: var(--dark-color);
    color: var(--light-color);
    padding: 4rem 0 2rem; /* Increased top padding, reduced bottom */
    margin-top: 4rem; /* Add space above the footer */
  }

  .footer-content {
    display: grid;
    grid-template-columns: repeat(
      auto-fit,
      minmax(250px, 1fr)
    ); /* Responsive columns */
    gap: 3rem; /* Increased gap */
    margin-bottom: 3rem; /* Increased bottom margin */
    align-items: start; /* Align items to the top */
  }

  .footer-logo h3 {
    font-family: "Playfair Display", serif;
    font-size: 1.8rem; /* Match header logo size */
    font-weight: 700;
    margin-bottom: 0.5rem; /* Reduced margin */
    color: var(--white-color);
  }

  .footer-logo p {
    font-size: 0.9rem;
    opacity: 0.8;
  }

  .footer-links h4,
  .footer-contact h4 {
    font-size: 1.2rem; /* Slightly larger heading */
    margin-bottom: 1.5rem; /* Increased margin */
    color: var(--white-color);
    position: relative;
    padding-bottom: 0.5rem;
  }

  /* Underline effect for footer headings */
  .footer-links h4::after,
  .footer-contact h4::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 30px;
    height: 2px;
    background-color: var(--accent-color);
  }

  .footer-links ul {
    list-style: none;
  }

  .footer-links li {
    margin-bottom: 0.8rem; /* Increased spacing */
  }

  .footer-links a {
    color: var(--light-color);
    opacity: 0.8;
    transition:
      opacity 0.3s ease,
      color 0.3s ease; /* Smooth transition */
  }

  .footer-links a:hover {
    color: var(--white-color);
    opacity: 1;
    padding-left: 5px; /* Subtle hover effect */
  }

  .footer-contact p {
    margin-bottom: 0.8rem; /* Increased spacing */
    font-size: 0.9rem;
    opacity: 0.8;
  }

  .footer-bottom {
    text-align: center;
    padding-top: 2rem; /* Add padding */
    border-top: 1px solid rgba(255, 255, 255, 0.1); /* Subtle separator */
    font-size: 0.9rem;
    opacity: 0.7;
  }

  /* Responsive Footer */
  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr; /* Stack columns on smaller screens */
      text-align: center; /* Center text */
    }

    .footer-links h4::after,
    .footer-contact h4::after {
      left: 50%; /* Center the underline */
      transform: translateX(-50%);
    }

    .footer-links ul {
      padding-left: 0; /* Remove default padding */
    }
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    .header .container {
      flex-direction: column;
      gap: 1rem;
    }

    nav ul {
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .logo a {
      color: var(--dark-color);
    }

    nav a {
      color: var(--dark-color);
    }

    .header {
      position: relative;
      background-color: var(--light-color);
    }
  }
</style>
